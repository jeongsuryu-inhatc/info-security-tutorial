# 11주차 침해사고 로그 번들

가상 시나리오: 새벽에 관리자 계정으로 대량 주문 취소가 발생했다. 아래 3개 로그로
침해 타임라인을 재구성하라.

| 파일 | 내용 |
|---|---|
| `web-access.log` | 웹서버 접근 로그 (Apache combined 유사) |
| `auth.log` | 인증/애플리케이션 로그 |
| `db-query.log` | DB 쿼리 로그 |

8주차에서 생성한 `week08/access.log`를 함께 분석에 포함할 수 있다.

> 등장 IP는 문서용 예약대역(198.51.100.0/24, 203.0.113.0/24)이다. 실제 조직과 무관하다.
